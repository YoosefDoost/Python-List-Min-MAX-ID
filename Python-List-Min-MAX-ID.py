# %% Find the min and max ID in a list
# Use LIST.index(LIST_ITEM) to find the ID of LIST_ITEM
LIST = [-3,-2,-1,0,1,2,3]
MAX = max(LIST)
MIN = min(LIST)

MAX_ID = LIST.index(MAX)
MIN_ID = LIST.index(MIN)

max = LIST[MAX_ID]
min = LIST[MIN_ID]
